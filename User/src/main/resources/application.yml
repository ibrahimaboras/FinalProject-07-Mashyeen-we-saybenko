server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://db:5432/userdb
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

booking:
  service:
    url: http://bookingservice:8081  # Optional for future use
